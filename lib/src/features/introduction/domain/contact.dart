import 'package:flutter/material.dart';

class Contact {
  final String tooltip;
  final String url;
  final IconData icon;

  const Contact({
    required this.tooltip,
    required this.url,
    required this.icon,
  });
}
